{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "cd website && hugo serve --minify -DFE -w",
    "serve:with-pagefind": "cd website && hugo --baseURL=/ --theme=docsy && npm_config_yes=true npx pagefind --site 'public' --output-subdir '../static/pagefind' && cd .. && npm run serve"
  },
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "hugo-extended": "^0.133.0",
    "postcss": "^8.4.41",
    "postcss-cli": "^9.1.0"
  }
}
